# Blank WALMI state

This directory is safe source-controlled starter material, not a WALMI person's
save. `shared/SHARED-STATE.json` contains no events and grants no publishing or
private-state authority.

Create private identity, experience, weights, and simulator saves only in an
ignored runtime directory. Never replace the blank fixture with a real person's
state.
